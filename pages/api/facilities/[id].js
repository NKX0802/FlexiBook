// API: GET /api/facilities/[id] — return full details of one facility
// Reads from "facilities" table by facility_id
// Returns: { facility_id, facility_name, facility_capacity, facility_type, facility_description, facility_status, facility_image_url }
// Returns 404 if facility not found

export default async function handler(req, res) {}
