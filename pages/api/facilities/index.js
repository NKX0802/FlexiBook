// API: GET /api/facilities — return list of all active facilities
// Supports query params: ?search=name&capacity=10&type=room
// Reads from "facilities" table
// Returns: [{ facility_id, facility_name, facility_capacity, facility_type, facility_status, facility_image_url }, ...]

export default async function handler(req, res) {}
